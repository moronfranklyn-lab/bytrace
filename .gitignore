# deps
node_modules/

# next
.next/
out/
next-env.d.ts

# typescript incremental build cache
tsconfig.tsbuildinfo
*.tsbuildinfo

# local db
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# env
.env.local
.env*.local

# misc
.DS_Store
*.log
npm-debug.log*
.idea/
.vscode/

# stray root db (real db lives in data/)
/autoarticle.db

# local build caches (node-gyp / npm) redirected into workspace for sandboxed builds
.cache/

# local env backup
.env.local.backup-*

# internal working notes (not part of the deliverable)
internal/
