# 项目状态

> 模板来源：`项目开发助手_副本2/ai-product-starter-kit-main 2/AGENTS.md` 第 8 节。
> **密钥、AccessKey、邀请码本身不写进这个文件。**
> 每次会话开始先读这个文件接着做。

---

## 当前阶段

- **阶段**：方案一 · 补齐项目文档（进行中）
- **最近更新**：2026-09-24
- **上次停在**：行动大纲已确认，正在产出 9 份文档
- **下一步**：方案一收尾汇报 → 你确认后进方案二（API 改 `.env`）

### 四方案总进度

| 方案 | 内容 | 状态 |
| --- | --- | --- |
| 方案一 | 补齐项目文档（PRD / 架构 / API / 数据模型 / 技术栈 / 环境变量 / 部署 / 项目状态） | 🟡 **进行中** |
| 方案二 | API 改 `.env`（主 Agent / 联网事实搜索 / 深度调研 三组独立） | ⬜ 未开始 |
| 方案三 | 本机客户端化（一键装 + 双击启动 + 数据搬标准位置 + doctor 自检） | ⬜ 未开始 |
| 方案四 | 套壳分发（Electron/Tauri `.app` + DMG）· 爬虫接入点预留 | ⬜ 本轮不做 |

---

## 决策台账（不超过 10 项）

| 编号 | 决策 | 状态 | 选定结论 | 依据 |
| --- | --- | --- | --- | --- |
| D1 | 改造对象 | ✅ 已定 | 只改 `autoarticle/`，不动 `agent-blueprint-main_副本` | 用户确认 |
| D2 | 客户端形态 | ✅ 已定 | 先做**本地服务 + 可双击启动器**；Electron/Tauri 放最后 | 用户确认（选推荐项） |
| D3 | 新名字 | ✅ 已定 | **笔迹 / ByTrace** | 用户确认 |
| D4 | API 外置范围 | ✅ 已定 | **只做「主 Agent」+「联网事实搜索」+「深度调研」**；配图 API 与爬虫本轮不做；爬虫由用户自备 GitHub 方案交主 Agent 调用 | 用户确认 |
| D5 | 旧品牌清理范围 | ✅ 已定 | 改工具名（AutoArticle→ByTrace）、代码模块名（`xiaopu-writing.ts`→`writing-rules.ts`）、资产目录名（`xiaopu-skill`→`author-skill`）；**prompt 里的「小普」作者人设表述一律不动**；**博主风格指纹命名一律不动** | 用户明确："风格 博主指纹不算" |
| D6 | 落地方式 | ✅ 已定（建议） | 在 `autoarticle/` **原地改**（有 git 可回滚，避免分裂现有 22 篇语料 + 指纹库） | 待用户最终确认（Q2） |
| D7 | `data/xiaopu-article-kb/` 目录名 | ⏳ 待确认 | 建议改名为 `author-kb`（内容不动） | 待用户答 Q1 |
| D8 | 主 Agent / 搜索 / 调研 接哪几家 API | ⏳ 待确认 | 用户未定则按兼容 6 家预留（Anthropic / OpenAI / DeepSeek / Kimi / GLM / MiniMax） | 待用户答 Q3 |
| D9 | 仓库文件夹名是否改 `bytrace/` | ⏳ 待确认（建议不改） | UI 名字改了就够了；改文件夹名要动桌面启动器与路径记录 | 行动大纲 §4 |
| D10 | critic 阈值 20/25 是否合理 | ⏳ 待确认 | 建议方案二冒烟时用 5-10 条真实样例标定 | PRD §4.4 / C1 |

---

## 待你拍板（阻塞项）

| # | 问题 | 我的建议 | 影响 |
| --- | --- | --- | --- |
| Q1 | `data/xiaopu-article-kb/` 目录名改成 `author-kb`？内容不动 | 改 | 方案一收尾要动这步 |
| Q2 | 原地改 `autoarticle/` 还是复制成 `bytrace/`？ | 原地改 | 决定整个改造的目录结构 |
| Q3 | 主 Agent / 搜索 / 调研接哪几家 API？（注：「调研」这一组已随 `/research` 删除而取消，实际只剩 主 Agent + 联网事实搜索 两组） | 按兼容 6 家预留 | 决定方案二预留的变量清单 |
| Q4 | 仓库文件夹名 `autoarticle/` 要不要也改？ | 暂不改 | 影响桌面启动器与路径记录 |
| **Q5** | **🔴 有两个项目副本，本会话动的是哪一个？** 见下方专节 | 需要你确认真实工作副本 | **直接决定改哪份代码** |

---

## 🔴 Q5 专节：存在两个项目副本，本会话操作的是外置卷那份

本会话的工作目录是 **`/Volumes/XiaoBeiDev/资料合集/项目合集/公众号/autoarticle`**（外置卷）。
但项目内所有旧文档（`CLAUDE.md` / `AGENTS.md` / `DEPLOY.md` 的路径示例）写的都是
**`/Users/mixingtumima0000/资料合集/项目合集/公众号/autoarticle`**（内置盘）。

实测两者是**不同的物理存储**（inode 设备号不同），且**内置盘那份的 `docs/` 里没有本会话新建的任何文件**：

```
$ stat -f "%d:%i %N" /Volumes/XiaoBeiDev/.../docs/PRD.md
16777249:547955 /Volumes/XiaoBeiDev/.../docs/PRD.md        ← 本会话新建

$ ls /Users/mixingtumima0000/资料合集/项目合集/公众号/autoarticle/docs/PRD.md
No such file or directory                                   ← 内置盘那份没有
```

`/Users/mixingtumima0000/资料合集/项目合集/公众号/` 目录**确实存在**（还有一份 305MB 的 `公众号.zip`，
以及一个指向 `/Volumes/XiaoBeiDev/项目/小项目` 的符号链接，说明你确实在用这块外置卷）。

**为什么这件事重要**：如果不确认清楚就往下做方案二 / 方案三，会出现两种后果之一：
1. 我改的是外置卷那份，但你双击启动器跑的是内置盘那份 → **你看到的效果和我的汇报对不上**；
2. 两份同时存在且都被改过 → 数据（`data/autoarticle.db`、22 篇语料、指纹库）**分叉成两套**。

**需要你做的一件事**：打开你平时用的那份，告诉我它的完整路径；或者告诉我内置盘那份已经不用了可以忽略。

---

## 上机环境实测（与旧文档不符的地方，供参考）

| 项 | 旧文档说 | 实测 |
| --- | --- | --- |
| Node.js 最低版本 | ≥ 18（`CLAUDE.md:543`） | **真实需 ≥ 22**（`got@15` engines `>=22`；`jsdom@29` 要 `^20.19 \|\| ^22.13 \|\| >=24`）。本机 **v24.18.1** / npm **10.8.2**，与 `STATUS.md` 记的 npm 11.12.1 不符（环境重置过） |
| 桌面启动器 | `~/Desktop/AutoArticle.command` 存在且已 chmod +x（`CLAUDE.md:516`） | **不存在**（`ls ~/Desktop/*.command` 无、仓库内 `find -name "*.command"` 无）。当前只能 `npm run dev -- -p 3100` |
| 当前模型通道 | 正文走 Claude Sonnet 4.6 | **`.env.local` 里唯一非空项是 `AUTOARTICLE_LLM_PROVIDER=codex-cli`** → 当前生成实际全走 Codex CLI |
| 项目根 | `/Users/mixingtumima0000/资料合集/...` | 见上方 Q5 |
| 仓库根有个 0 字节 `autoarticle.db` | 未提 | 存在，与真正的 `data/autoarticle.db` **无关**，别误当数据库（未删） |
| `npx vitest run` 可跑单测 | `CLAUDE.md:578` 有 | **不可执行**：vitest 未装、无 `test` script、**全仓零个测试文件**。当前唯一质量 gate 只有 `npx tsc --noEmit`（本会话已跑，**0 error**） |
| `npm run lint` 可用 | `package.json` 有 `lint` script | **不可用**：eslint 未装，`next lint` 在 Next 15 已 deprecated |

> **⚠️ 工作树警告**：`git status --short` 有 **93 项未提交改动**（含 9 项删除），这是 2026-07-07 那次全项目审查修复轮的成果，**尚未 commit**。
> 千万别在这个状态下执行 `git reset --hard` 或 `git checkout .`——会把这批修复整个销毁。
> 方案二开工前应先把这批改动 **commit 一次**作为基线。

---

## 已发现的现存问题（改造前基线）

| 编号 | 问题 | 等级 | 计划在哪个方案修 |
| --- | --- | --- | --- |
| P1 / R1 / A1 | `lib/claude.ts` 硬编码本机 CLI 路径 | 🔴 | 方案二 |
| P2 / R2 / A2 | `lib/db.ts` 用 `process.cwd()` 定位 DB | 🔴 | 方案三 |
| P3 / R3 / A3 | 环境变量三套命名并存 | 🟡 | 方案二 |
| P4 / A4 | 模型层无功能分组 | 🟡 | 方案二 |
| P5 / A8 | 无 `/api/health`、无配置向导 | 🟡 | 方案三 |
| P6 | 品牌名分散（页脚 4 / 导航 3 / metadata / 系统提示词） | 🟢 | 方案一（仅文档）/ 后续代码 |
| P7 | `xiaopu` 命名残留（7 处引用 + 2 个目录名） | 🟢 | 方案一（目录/文档）/ 后续代码 |
| A5 | `app/compose/page.tsx` ~3100 行违反自家分层 | 🟡 | 本轮不修 |
| A6 | v3 POST route 与 engine 复制体漂移 | 🟡 | 本轮不修 |
| A7 | 模型侧无成本护栏 | 🟡 | 方案二建议补 |
| A9 | 无登录，默认绑本机 | 🟢 | 方案三：启动器强制 `127.0.0.1` |

### 本轮文档工作中查出的"旧文档已过期"事实（重要）

| 旧文档说法 | 实际情况 | 证据 |
| --- | --- | --- |
| Apify 是爬虫兜底通道，有 `APIFY_TOKEN` 与 `/api/apify/usage` | **Apify 模块已被删除**：`lib/crawler/apify.ts` / `lib/apify/usage.ts` / `app/api/apify/usage/route.ts` / `components/nav/ApifyStatusPill.tsx` 在 git 中显示为 `D`。爬虫只走 OpenCLI → cheerio → 手贴 | `git status` |
| `settings` 表可存 `apify_token` | **`ALLOWED_KEYS` 只剩 `default_theme`** | `app/api/settings/route.ts:17` |
| 配方可跨博主复用（`style_recipes`） | 表还在，但**PATCH 重提炼会换碎片 id 导致配方静默失效**（技术债） | `CLAUDE.md` 2026-07-07 节 |
| API 路由约 20 个 | **实际 32 个** `route.ts` | `find app/api -name route.ts \| wc -l` |
| 页面路由约 15 个 | **实际 18 个** `page.tsx` | `find app -name page.tsx \| wc -l` |
| `npx vitest run` 可跑单测（`CLAUDE.md:578`） | **vitest 未安装**：`package.json` 无 `test` script、无 `vitest.config.*`、**全仓零个 `*.test.ts` / `*.spec.ts`**。该命令当前失效 | 实测 `ls node_modules/.bin/vitest` 不存在 |
| `lib/compose-schema.ts` 是「生成流程的 Zod schema」（`CLAUDE.md:357`） | **zod 未安装、全仓零 import**。该文件实际只有一个 `ensureComposeColumns()` 幂等扩列函数 | 实测读文件 |
| `npm run lint` 可用 | **eslint 未安装**，`next lint` 在 Next 15 已 deprecated | `CLAUDE.md` 自述 + 实测 |
| Node ≥ 18 即可（`CLAUDE.md:543`） | **实际需 ≥ 22**（`got@15` 要 `node >=22`；`jsdom@29` 要 `^20.19 \|\| ^22.13 \|\| >=24`）。本机实测 Node **v24.18.1** / npm **10.8.2**（与 `STATUS.md` 记的 npm 11.12.1 不符，说明环境重置过） | 实测 + `package-lock.json` engines |
| 首页显示「本地已有 247 张配图」 | **硬编码文案**，数据库 `local_assets` 实际 **21 行** | 实测 `sqlite3 ... count(*)` |

---

## 方案一产出清单

| 文件 | 状态 | 内容 |
| --- | --- | --- |
| `docs/行动大纲.md` | ✅ 已完成 | 四方案行动大纲 + 现状盘点 + 四条决策 + 待拍板项 |
| `docs/PRD.md` | ✅ 已完成 | 回溯性 PRD，按 8 字段模板，含 critic 真实阈值与反爬边界 |
| `docs/ARCHITECTURE.md` | ✅ 已完成 | 七层架构 + mermaid 图 + 数据流 + 17 组件选型对照 + 权限/可观测性 |
| `docs/ENV.md` | ✅ 已完成 | 当前代码实际读取的全部环境变量 + 四 provider 归一规则 |
| `docs/API.md` | ✅ 已完成 | 32 个 route 的契约 + 20 个 SSE 事件总表 + 11 条文档冲突 + 9 条内部不一致 |
| `docs/DATA-MODEL.md` | ✅ 已完成 | 21 张表 / 列 / 索引 / ER 图 / 14 组 JSON 列 / 迁移规则 |
| `docs/TECH-STACK.md` | ✅ 已完成 | 版本清单 + 选型理由 + "为什么不" + 技术债 + 11 条漂移 |
| `docs/DEPLOY.md` | ✅ 已完成 | 安装 / 启动 / 配置 / 备份 / 排错 / 升级回滚 + 12 条待核实 |
| `docs/项目状态.md` | ✅ 本文件 | 进度 + 决策台账 + Q5 双副本问题 + 环境实测 |

---

## 阶段闸门是否表（方案一）

| # | 检查项 | 是 / 否 |
| --- | --- | --- |
| 1 | 本阶段范围内的产出（文档）已全部生成 | ✅ 是（**9/9**，共 6179 行） |
| 2 | 文档内容逐条有源码证据，无臆造 | ✅ 是（关键事实均标注文件/行号，并由主线程独立复核） |
| 3 | 文档之间事实一致（路由数、表数、通道、超时值） | ✅ 是（已修正 Apify / research / 路由数 / 超时值 / s16 五处不一致） |
| 4 | 未改动任何现有代码 / 数据 | ✅ 是（`find app lib components stores hooks scripts -newermt 12:10` 为空；git 改动数始终 93） |
| 5 | 未把密钥写进文档 | ✅ 是（`.env.local` 只描述非空项为 `codex-cli`，未复述任何 key 值） |
| 6 | 已向 Ethan 汇报产出与待拍板项 | ✅ 是 |

---

## 变更记录

| 日期 | 做了什么 |
| --- | --- |
| 2026-09-24 | 建立本状态文件；创建行动大纲；完成 PRD / 架构 / 环境变量三份文档；启动 API / 数据模型 / 技术栈 / 部署四份文档的子任务 |
